# Aether public Pages mirror

This directory contains the published static runtime for the private source repository `fabiodr/reducewatch-proto1`.

- Supported URL: `https://fabiodr.github.io/reducewatch-proto1/`
- Source changes belong in the private source repository, not in this mirror.
- `deployment.json` records source, candidate, merge, workflow, artifact, and exact asset identities.
- The verification workflow checks the real HTTPS origin and uploads read-only evidence. It must not commit or push a verification result.
- Runtime files must come from the exact retained source CI artifact; do not rebuild or hand-edit them here.
- Roll back by restoring a previously verified mirror commit or republishing a previously retained artifact.

The climate values shown by Aether are illustrative and not decision-grade science.
